apiVersion: v1
kind: Service
metadata:
{{- include "metadata" . }}
spec:
  selector:
    app.kubernetes.io/name: {{ .Values.name }}
  ports:
    - targetPort: {{ dig "SERVER_PORT" "" .Values.env }}
      protocol: TCP
      name: web